# GUI работает только на стандартной библиотеке (tkinter уже встроен в python),
# но для рекордера нужны эти пакеты:

mss>=9.0.1
pillow>=10.0.0
pynput>=1.7.6

# для сборки .exe (опционально, только разработчикам):
pyinstaller>=6.0.0

openai>=1.50.0